# verify — a thin wrapper. The procedure itself lives in .rein/prompts/, which every host reads.
description = "Verification. Run and record functional and non-functional tests, then ask for the acceptance decision."
prompt = """
Read the file `.rein/prompts/commands/verify.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.gemini/rein.md`.
"""
