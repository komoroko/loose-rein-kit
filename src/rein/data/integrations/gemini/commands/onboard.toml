# onboard — a thin wrapper. The procedure itself lives in .rein/prompts/, which every host reads.
description = "Brownfield onboarding. Survey the existing codebase read-only and map it into docs/05-current-state.md so cycles can proceed on the template."
prompt = """
Read the file `.rein/prompts/commands/onboard.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.gemini/rein.md`.
"""
