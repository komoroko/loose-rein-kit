# status — a thin wrapper. The procedure itself lives in .rein/prompts/, which every host reads.
description = "Progress dashboard. Show current phase, gate approval status, and task progress at a glance."
prompt = """
Read the file `.rein/prompts/commands/status.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.gemini/rein.md`.
"""
