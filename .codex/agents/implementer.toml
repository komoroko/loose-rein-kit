# Loose Rein role agent. The body lives in the SSOT prompts, not here — the same one-line
# pointer the Claude Code and Copilot role files carry, so one role has one definition.
name = "implementer"
description = "Implements a single task ticket (T-NNN), writes unit/integration tests that satisfy its acceptance criteria, and gets them green. Delegated one task at a time from the /build loop."
sandbox_mode = "workspace-write"
developer_instructions = "Your full role definition is in `.rein/prompts/agents/implementer.md`. Read that file first and follow it exactly."
