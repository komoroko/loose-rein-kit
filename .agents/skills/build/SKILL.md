---
name: build
description: Phase 4 implementation. Consume tasks autonomously (`autonomous-build-iteration`). Each task's condition to advance is green tests.
---

Read the file `.rein/prompts/commands/build.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.codex/rein.md`.
