# design — a thin wrapper. The procedure itself lives in .rein/prompts/, which every host reads.
description = "Design. Build an approach from the claims and let the human decide the technical choices."
prompt = """
Read the file `.rein/prompts/commands/design.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.gemini/rein.md`.
"""
