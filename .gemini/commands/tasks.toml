# tasks — a thin wrapper. The procedure itself lives in .rein/prompts/, which every host reads.
description = "Task breakdown. Split the work into task tickets, then present the mandate for approval."
prompt = """
Read the file `.rein/prompts/commands/tasks.md` and execute that procedure exactly. Capability terms resolve per the capability mapping in `.gemini/rein.md`.
"""
