# Loose Rein — Gemini CLI entry point

The operating rules live in `AGENTS.md` and the Gemini capability mapping in `.gemini/rein.md`
— both imported below. This file exists only so that Gemini CLI, which reads `GEMINI.md` rather
than `AGENTS.md`, loads them at all.

@AGENTS.md

@.gemini/rein.md
